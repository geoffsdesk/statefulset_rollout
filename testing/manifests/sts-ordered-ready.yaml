apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-sts-ordered
  namespace: default
  labels:
    app: test-sts-ordered
    test-variant: ordered-ready-negative-control
spec:
  replicas: 10
  podManagementPolicy: OrderedReady
  serviceName: test-sts-ordered
  selector:
    matchLabels:
      app: test-sts-ordered
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: "20%"
  template:
    metadata:
      labels:
        app: test-sts-ordered
    spec:
      terminationGracePeriodSeconds: 5
      containers:
      - name: nginx
        image: nginx:1.27.0
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 2
          periodSeconds: 3
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            cpu: 50m
            memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: test-sts-ordered
  namespace: default
spec:
  clusterIP: None
  selector:
    app: test-sts-ordered
  ports:
  - port: 80
    targetPort: 80
