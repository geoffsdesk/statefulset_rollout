apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-sts-ondelete
  namespace: default
  labels:
    app: test-sts-ondelete
    test-variant: ondelete-alternative
spec:
  replicas: 115
  podManagementPolicy: Parallel
  serviceName: test-sts-ondelete
  selector:
    matchLabels:
      app: test-sts-ondelete
  updateStrategy:
    type: OnDelete
  template:
    metadata:
      labels:
        app: test-sts-ondelete
    spec:
      terminationGracePeriodSeconds: 5
      containers:
      - name: nginx
        image: nginx:1.27.0
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 2
          periodSeconds: 3
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            cpu: 50m
            memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: test-sts-ondelete
  namespace: default
spec:
  clusterIP: None
  selector:
    app: test-sts-ondelete
  ports:
  - port: 80
    targetPort: 80
